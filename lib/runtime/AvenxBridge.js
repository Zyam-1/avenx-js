/**
 * Base class for all shared reactive states (bridges).
 * Provides a foundation for building complex shared logic and data.
 */
export class AvenxBridge {
    constructor() {
        // Initial state can be defined in the constructor of the extending class.
    }
}
